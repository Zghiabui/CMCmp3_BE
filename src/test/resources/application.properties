# --- DATABASE ?O (H2) ---
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=never

# --- CÁC BI?N MÔI TR??NG GI? L?P (QUAN TR?NG) ---
# S?a JWT_SECRET thành jwt.secret ?? kh?p v?i code @Value("${jwt.secret}")
jwt.secret=test-secret-key-must-be-very-very-very-long-to-pass-validation-12345
jwt.expiration=86400000

# Các c?u hình khác c?ng c?n ?i?n giá tr? gi?
spring.security.oauth2.client.registration.google.client-id=test-google-id
spring.security.oauth2.client.registration.google.client-secret=test-google-secret
spring.mail.username=test-email@gmail.com
spring.mail.password=test-email-password
firebase.bucket.name=test-bucket-name

# ???ng d?n file Firebase gi? (?? tránh l?i FileNotFound)
# L?u ý: Trong môi tr??ng test, ta tr? t?m nó vào file nào ?ó có s?n ho?c ?? tr?ng n?u code không check ch?t
firebase.service-account.path=classpath:application.properties