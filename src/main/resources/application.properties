spring.application.name=CMCmp3

spring.datasource.url=jdbc:mysql://localhost:3306/musicapp?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=01042005a5
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# ? JPA & HIBERNATE CONFIGURATION
# ===============================
spring.jpa.hibernate.ddl-auto=update         
spring.jpa.show-sql=true                
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===============================
# ? JWT CONFIGURATION (tu? ch?n)
# ===============================
jwt.secret=your-256-bit-secret-your-256-bit-secret
jwt.expiration=86400000                      

# ===============================
# ? SERVER CONFIGURATION
# ===============================
server.port=8080                            
server.error.include-message=always         

# ===============================
# ? CORS CONFIGURATION (n?u dùng frontend riêng)
# ===============================
# spring.web.cors.allowed-origins=http://localhost:3000
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE